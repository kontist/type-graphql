import { TargetMetadata } from "@src/metadata/storage/definitions/common";

// eslint-disable-next-line
export default interface ResolverMetadata extends TargetMetadata {
  // TODO: maybe more fields in the future, like "namespace" or sth
}
